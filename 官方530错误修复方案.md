# ✅ Notion API 530 错误 - 官方修复方案

## 🎯 问题根源

Notion 更改了 API 端点名称：
- **旧端点**: `/api/v3/syncRecordValues` ❌
- **新端点**: `/api/v3/syncRecordValuesMain` ✅

当使用旧端点时，Notion 返回 530 错误。

## 🔧 官方修复方案

根据官方 PR #3568 和 Issue #3566，修复方法非常简单：

### 修改 `lib/notion/getNotionAPI.js`

添加 `kyOptions` 配置，在请求前自动替换 URL：

```javascript
import { NotionAPI as NotionLibrary } from 'notion-client'
import BLOG from '@/blog.config'

const notionAPI = getNotionAPI()

function getNotionAPI() {
  return new NotionLibrary({
    activeUser: BLOG.NOTION_ACTIVE_USER || null,
    authToken: BLOG.NOTION_ACCESS_TOKEN || BLOG.NOTION_TOKEN_V2 || null,
    userTimeZone: Intl.DateTimeFormat().resolvedOptions().timeZone,
    kyOptions: {
      // 全局 Ky 配置
      hooks: {
        beforeRequest: [
          (request) => {
            const url = request.url.toString()
            if (url.includes('/api/v3/syncRecordValues')) {
              return new Request(
                url.replace('/api/v3/syncRecordValues', '/api/v3/syncRecordValuesMain'),
                request
              )
            }
            return request
          }
        ]
      }
    }
  })
}

export default notionAPI
```

## ⚙️ 配置说明

### 方式 1: 使用 NOTION_TOKEN_V2 (推荐)

`.env.local`:
```bash
NOTION_TOKEN_V2=你的token_v2值
```

获取方法：
1. 访问 https://www.notion.so 并登录
2. F12 → Application → Cookies → notion.so
3. 复制 `token_v2` 的值

### 方式 2: 公开数据库

如果不想使用 token，确保 Notion 数据库设置为公开访问。

## 📝 完整修改清单

### ✅ 已完成的修改

1. **创建** `lib/notion/getNotionAPI.js` - 全局 NotionAPI 单例
2. **更新** `lib/notion/getPostBlocks.js` - 使用全局 notionAPI 实例
3. **更新** `lib/notion/getPageProperties.js` - 使用全局 notionAPI 实例
4. **更新** `blog.config.js` - 添加 NOTION_TOKEN_V2 配置
5. **配置** `.env.local` - 设置 NOTION_TOKEN_V2

### 🔑 关键改进

**API 端点修复**：
- ✅ 自动将 `syncRecordValues` 替换为 `syncRecordValuesMain`
- ✅ 使用 Ky hooks 在请求前拦截和修改 URL
- ✅ 对所有 Notion API 请求生效

**架构改进**：
- ✅ 全局单例模式，避免重复创建 API 实例
- ✅ 统一的配置管理
- ✅ 更好的错误处理和重试机制

## 🚀 测试

启动开发服务器：
```powershell
pnpm dev
```

访问：http://localhost:3001

### 成功标志

✅ 控制台无 530 错误
✅ 控制台显示 `[响应成功]`
✅ 页面正常加载 Notion 内容
✅ 文章列表正常显示

### 失败排查

如果仍有问题：

1. **检查 Token**
   - 确认 `.env.local` 中 `NOTION_TOKEN_V2` 设置正确
   - Token 没有多余空格
   - Token 未过期

2. **检查文件**
   - `lib/notion/getNotionAPI.js` 包含 `kyOptions` 配置
   - 所有相关文件都导入了 `notionAPI`

3. **重启服务器**
   ```powershell
   # Ctrl+C 停止
   pnpm dev
   ```

## 📚 参考资料

- **官方 PR**: https://github.com/tangly1024/NotionNext/pull/3568
- **相关 Issue**: 
  - https://github.com/tangly1024/NotionNext/issues/3566
  - https://github.com/tangly1024/NotionNext/issues/3563
- **上游问题**: https://github.com/NotionX/react-notion-x/issues/659

## 💡 工作原理

```
请求流程：
1. NotionAPI 准备请求 /api/v3/syncRecordValues
2. beforeRequest hook 拦截请求
3. 检测 URL 包含 'syncRecordValues'
4. 替换为 'syncRecordValuesMain'
5. 发送修改后的请求到 Notion
6. Notion 返回 200 成功响应 ✅
```

## 🎉 总结

这是官方认可的**简单粗暴但有效**的临时修复方案：

✅ **快速**: 只需修改一个文件
✅ **有效**: 已被社区验证
✅ **兼容**: 不影响现有功能
✅ **透明**: URL 替换在后台自动完成

长期方案是迁移到官方 Notion API (Integration)，但这需要更大的重构工作。当前方案可以让网站立即恢复正常运行。
